# 通用代码任务 Skill

适用于默认代码修改、Bug 修复、小型功能开发和评审任务。

## 流程

1. 读取开发者原始需求、目标仓库元信息和检索到的知识片段。
2. 优先遵守 `AGENTS.md`、`README.md` 和仓库文档中的约束。
3. 输出统一 diff，不直接修改文件。
4. 给出变更说明、风险说明和建议校验命令。
5. 若校验失败，结合失败日志重新生成修复 diff。

## 输出要求

- 必须包含可审阅 diff。
- 必须说明影响范围。
- 必须列出回归风险。
- 必须列出已执行或建议执行的校验命令。
